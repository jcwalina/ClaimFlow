services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: claimflow
      POSTGRES_PASSWORD: claimflow
      POSTGRES_DB: claimflow
    ports:
      - "5432:5432"
    volumes:
      - claimflow_pg:/var/lib/postgresql/data
volumes:
  claimflow_pg:
